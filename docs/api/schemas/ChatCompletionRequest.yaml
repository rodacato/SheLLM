type: object
required: [model, messages]
properties:
  model:
    type: string
    description: Provider name or alias
    example: claude
  messages:
    type: array
    minItems: 1
    items:
      $ref: "./ChatMessage.yaml"
  max_tokens:
    type: integer
    minimum: 1
    maximum: 128000
