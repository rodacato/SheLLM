type: object
required: [ref]
properties:
  ref:
    type: string
    pattern: '^v[0-9]+\.[0-9]+\.[0-9]+$'
    example: v1.4.0
  confirm:
    type: string
    description: Required, and equal to `ref`, when the target is a major version jump.
