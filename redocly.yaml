apis:
  main:
    root: docs/api/openapi.yaml

extends:
  - recommended

rules:
  # The only server this spec can name is the caller's own machine — SheLLM is self-hosted.
  no-server-example.com: off
